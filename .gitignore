__pycache__/
*.py[co]
*.egg-info/
.ipynb_checkpoints/

# Build artifacts / heavy outputs — regenerated by the pipeline, not committed
output/
logs/
*.npy
*.npz
*.pt
*.pth
*.ckpt
*.safetensors
*.bin
*.mp4
*.webm
*.mkv
*.avi
*.mov

# External dependency cloned at install time, not vendored (also matches the local
# convenience symlink that points at a working WHAM checkout)
external/WHAM
yolo11x-pose.pt

# VideoMAE baseline training tree — local only; reproduction uses the precomputed
# reproduce/lma_data/videomae_preds.csv, not this tree
train_videomae/

# Editor / OS noise
.vscode/
.idea/
.DS_Store
.claude/

# Ship the precomputed 61-feature reproduction data despite the rules above
!reproduce/lma_data/features.npz
